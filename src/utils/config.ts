const ENVIRONMENT = process.env.ENVIRONMENT || "development";
const TOKEN_NAME = "kukka-coin";
const ACCESS_TOKEN_SECRET_KEY = "q98wytpno7wfoubdn493h";

const config = {
  ENVIRONMENT,
  TOKEN_NAME,
  ACCESS_TOKEN_SECRET_KEY,
};

export default config;
